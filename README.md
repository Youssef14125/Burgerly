# Fast React Burger Co.

A React + Redux Toolkit + React Router + Tailwind app for ordering burgers.
Converted from a pizza-ordering app; menu data is static (`src/data/burgers.json`)
and orders are persisted to `localStorage` instead of a remote API.

## Run locally

npm install
npm run dev

Then open the printed local URL (usually http://localhost:5173).
